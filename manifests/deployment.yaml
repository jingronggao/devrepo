# Generated by IBM TransformationAdvisor
# Mon Dec 17 09:55:20 EST 2018

apiVersion: v1
kind: Service
metadata:
  name: modresortsjboss
  labels:
    app: modresortsjboss
spec:
  ports:
    - port: 9080
  selector:
    app: modresortsjboss
  type: NodePort

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: modresortsjboss
  name: modresortsjboss
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: modresortsjboss
    spec:
      containers:
      - image: mycluster.icp:8500/default/modresortsjboss:latest
        name: modresortsjboss
        env:
          - name: WLP_LOGGING_CONSOLE_FORMAT
            value: json
          - name: WLP_LOGGING_CONSOLE_LOGLEVEL
            value: info
          - name: WLP_LOGGING_CONSOLE_SOURCE
            value: message,trace,accessLog,ffdc
        ports:
        - containerPort: 9080
          protocol: TCP
